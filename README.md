# lancer une recette 
  
  ```
  ansible-playbook -i hosts playbooks/install.yml  
  ```

# créer un role 
  
  Ici le role "tools"
  ```
  ansible-galaxy init tools  
  ```

# créer un password

  ```
 ansible-vault encrypt_string  
  ```
